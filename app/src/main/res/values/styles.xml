<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="successDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/successDialogAnimation</item>
    </style>

    <style name="successDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/fromsmall</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="TextInputLayoutStyle" parent="Theme.AppCompat">
        <item name="boxStrokeColor">#000</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>

    <style name="AppTheme.changeEditText" parent="Theme.MaterialComponents.Light">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorPrice</item>
    </style>
</resources>
